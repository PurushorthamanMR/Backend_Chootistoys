// Home page section keys, in their original hard-coded order (Home.jsx before
// the drag-and-drop layout existed) - shared between the migration seed and
// the controller's validation so they can never drift apart.
const SECTION_KEYS = [
  'offer_banner',
  'hot_categories',
  'featured_products',
  'featured_categories',
  'subcategory_spotlight',
  'best_selling',
  'banner_carousel',
  'all_products',
];

module.exports = { SECTION_KEYS };
